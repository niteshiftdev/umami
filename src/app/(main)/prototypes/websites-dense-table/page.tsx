import { Metadata } from 'next';
import { WebsitesDenseTablePage } from './WebsitesDenseTablePage';

export default function () {
  return <WebsitesDenseTablePage />;
}

export const metadata: Metadata = {
  title: 'Websites - Dense Table',
};
